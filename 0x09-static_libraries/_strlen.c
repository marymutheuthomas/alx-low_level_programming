#include"main.h"

/**
 * _strlen - calculates the string length
 * @s: string to calculate the length
 * Return: return 1 or 0
 */

int _strlen(char *s)
{
	int i = 0;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}

